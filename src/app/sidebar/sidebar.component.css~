.sidebar-container {
  width: 100%;
  height: 95%;
  margin: 20px;
  border: 1px solid #555;
}

p {
  font-family: 'Montserrat', sans-serif;
  margin: 10px;
}

#subheading {
  font-family: 'Montserrat', sans-serif;
  margin: 10px;
  font-size: 20px;
}

#logo {
  display: block;
  margin-left: auto;
  margin-right: auto;
}

button {
  background:none;
  border:none;
  cursor: pointer;
  font-family: 'Montserrat', sans-serif;
  padding: 6px 8px 6px 16px;
  text-decoration: none;
  font-size:20px;
  color: #111;
  display: block;
  outline: transparent;
}

mat-drawer-content {
  overflow-x: hidden;
  position: relative;
  left: 15px; 
}

button:hover {
  color: #f1f1f1;
}
